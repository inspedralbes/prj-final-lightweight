PLAYWRIGHT_BASE_URL=http://localhost:5173

# Backend reachable directly on :3000 in local dev (Nginx adds /api only in prod).
PLAYWRIGHT_API_URL=http://localhost:3000

# The /testing/* endpoints used by fixtures (loginViaApi, resetDatabase) only exist when
# the backend is started with E2E_TESTING=true. See e2e/README.md → "Datos de prueba".
